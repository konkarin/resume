---
import "../styles/custom.css";
const { frontmatter } = Astro.props;
---

<!doctype html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width,height=device-height,initial-scale=1,maximum-scale=1,user-scalable=no,viewport-fit=cover" />
    <title>{frontmatter.title}</title>
  </head>
  <body class="markdown-body">
    <main class="container">
      <slot />
    </main>

    <script is:inline>
      // 印刷前にすべてのdetailsを開く
      window.addEventListener("beforeprint", function () {
        const details = document.querySelectorAll("details");
        details.forEach((detail) => {
          if (!detail.open) {
            detail.open = true;
            detail.dataset.wasClosedBeforePrint = "true";
          }
        });
      });

      // 印刷後に元の状態に戻す
      window.addEventListener("afterprint", function () {
        const details = document.querySelectorAll('details[data-was-closed-before-print="true"]');
        details.forEach((detail) => {
          detail.open = false;
          delete detail.dataset.wasClosedBeforePrint;
        });
      });
    </script>
  </body>
</html>

<style is:global>
  * {
    box-sizing: border-box;
  }

  .container {
    max-width: 752px;
    margin: 0 auto;
    padding: 2rem;
  }

  img {
    max-width: 100%;
    height: auto;
  }

  @media (max-width: 768px) {
    .container {
      padding: 1rem;
    }
  }

  .markdown-body li > a > p {
    margin-bottom: 0;
  }

  details > summary {
    margin-bottom: 1rem;
  }

  table {
    font-size: 0.9rem;
  }

  .anchor-link {
    position: absolute;
    top: 0px;
    left: 0px;
    margin-left: -0.87em;
    font-weight: 500;
    user-select: none;
    opacity: 0;
    text-decoration: none;
    transition:
      color 0.25s,
      opacity 0.25s;
  }
  h1,
  h2,
  h3,
  h4,
  h5,
  h6 {
    position: relative;
  }
  h1:hover > .anchor-link,
  h2:hover > .anchor-link,
  h3:hover > .anchor-link,
  h4:hover > .anchor-link,
  h5:hover > .anchor-link,
  h6:hover > .anchor-link {
    opacity: 1;
  }

  /* Remove depth-specific padding as nesting handles it */
  .toc .depth-2,
  .toc .depth-3,
  .toc .depth-4 {
    padding-left: 0;
  }

  .image-container {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  @media (min-width: 576px) {
    .image-container {
      flex-direction: row;
    }
    .image-container img {
      max-width: 300px;
    }
  }
</style>
